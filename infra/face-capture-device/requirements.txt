# Pinned to match backend/requirements.txt exactly, so on-prem detection
# behaves identically to the server-side Tier C pipeline.
opencv-python==4.10.0.84
dlib==19.24.6
face_recognition==1.3.0
numpy==1.26.4
requests>=2.31.0
